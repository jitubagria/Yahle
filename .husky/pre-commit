#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

if git diff --cached --name-only | grep -q "^\.env$"; then
  echo "❌ .env file should not be committed!"
  exit 1
fi

# Run lint-staged to auto-fix and check staged files
npx lint-staged
